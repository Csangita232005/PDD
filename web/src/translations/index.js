// Web Translations connected to Web Shared Translations Module inside src
import { translations as sharedTranslations } from '../shared/translations';

export const translations = sharedTranslations;
export default translations;
